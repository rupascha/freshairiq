# FreshAirIQ 0.25.0.67 — Public Beta Screenshot Hotfix

- Fixes README image rendering in HACS by using absolute raw GitHub image URLs.
- Keeps all eight Public Beta screenshots in `docs/screenshots/`.
- Adds a regression test that verifies every README image URL maps to a real repository file.
- No ventilation, learning, decision, diagnostics, or UI logic changed.
